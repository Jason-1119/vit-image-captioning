# vit-image-captioning
Built a multimodal Vision-Language Model integrating a Vision Transformer (ViT) encoder and an autoregressive Transformer decoder for image captioning. The system learns joint visual-text representations and generates captions conditioned on image features. Evaluated on Flickr8k using BLEU scores.
